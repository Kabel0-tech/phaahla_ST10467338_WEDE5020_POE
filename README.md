Created external `style.css` stylesheet and linked it across all 6 HTML pages.
Applied CSS Reset (`* { margin: 0; padding: 0; box-sizing: border-box; }`) for cross-browser consistency.
Implemented typographic scale using relative units (`rem`, `em`).
Built desktop layout using CSS Grid and Flexbox for product cards, menus, and forms.
Configured mobile responsiveness using CSS Media Queries (`@media (max-width: 768px)` and `@media (max-width: 480px)`).
Implemented interactive states (`:hover`, `:focus`, `:active`) on navigation links and CTA buttons.
